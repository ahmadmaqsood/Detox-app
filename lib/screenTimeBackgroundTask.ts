// Temporary stub: background screen-time task disabled because the
// native usage-stats module is not compatible with the current
// React Native New Architecture setup.
//
// Keeping the function exported so the rest of the app can call it
// safely without runtime errors.
export async function registerScreenTimeBackgroundTask(): Promise<void> {
  // No-op for now.
}
